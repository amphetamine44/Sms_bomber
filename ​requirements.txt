# Create the file with the content above
cat > requirements.txt << 'EOF'
requests>=2.25.0
aiohttp>=3.8.0
flask>=2.0.0
pytest>=7.0.0
pytest-asyncio>=0.18.0
EOF

# Add, commit, push
git add requirements.txt
git commit -m "Add complete requirements.txt for CI and deployment"
git push origin main
